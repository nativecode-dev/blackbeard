{
  "author": "support@nativecode.com",
  "description": "It's me app, matey...yarrrrrrr...",
  "license": "MIT",
  "main": "index.js",
  "name": "blackbeard",
  "private": true,
  "repository": "git@github.com:mikepham/blackbeard.git",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna bootstrap && lerna run build && ts-node chest tsconfig-types",
    "clean": "lerna run clean",
    "docker": "lerna run build --scope @nativecode/blackbeard.services && lerna run docker --scope=@nativecode/blackbeard.services",
    "test": "lerna run test",
    "dockerdev": "lerna run docker --scope=@nativecode/blackbeard.services && lerna run docker-run --scope=@nativecode/blackbeard.services",
    "startdev": "lerna run startdev --scope=@nativecode/blackbeard.services",
    "update-deps": "ts-node chest tsconfig-types"
  },
  "devDependencies": {
    "@types/node": "^8.0.50",
    "chalk": "^2.3.0",
    "lerna": "^2.5.1"
  }
}
