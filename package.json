{
  "author": "support@nativecode.com",
  "description": "It's me app, matey...yarrrrrrr...",
  "license": "MIT",
  "name": "blackbeard",
  "private": true,
  "repository": "git@github.com:mikepham/blackbeard.git",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna bootstrap && lerna run build && ts-node chest tsconfig-types",
    "clean": "lerna run clean",
    "docker": "lerna run build --scope @beard/services && lerna run docker --scope=@beard/services",
    "test": "lerna run test",
    "dockerdev": "lerna run docker --scope=@beard/services && lerna run docker-run --scope=@beard/services",
    "startdev": "lerna run startdev --scope=@beard/services",
    "update-deps": "ts-node chest tsconfig-types"
  },
  "devDependencies": {
    "@types/node": "^8.0.50",
    "chalk": "^2.3.0",
    "lerna": "^2.5.1"
  }
}
